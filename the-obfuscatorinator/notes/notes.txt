For Generic types that use a mystery generic and then get set later, the set type
is not recognized.

For example:
    Test<?> test = new Test<String>("Hi");

the type detected is Test<?> and not Test<String>




Interesting case:


int[] hi = {1,2,3};

int hi
        5



public void repaint(String color) {
        this.color = color;
    }

= color